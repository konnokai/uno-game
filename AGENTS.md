# UNO 網頁遊戲專案規格

## 專案目標

建立一款可在瀏覽器遊玩的即時多人 UNO 遊戲。

- 支援 2–8 位玩家。
- 房主建立房間後取得 6 碼房號與邀請連結。
- 玩家不需註冊，只需輸入暱稱。
- 採單回合決勝，先出完手牌者獲勝。
- 第一版以本機開發與執行為主。
- 桌面與手機皆須可正常操作。

## 技術架構

- 前端：React、TypeScript、Vite。
- 後端：Cloudflare Worker、SQLite-backed Durable Objects、原生 WebSocket。
- 測試：Vitest；主要瀏覽器流程使用 OpenChamber web browser tools 驗證。
- 套件管理與 Monorepo：pnpm workspace。
- 共用的卡牌、遊戲狀態、純房間服務及 WebSocket protocol 型別放在 shared package。
- 每個房間使用一個 SQLite-backed Durable Object；公開大廳清單使用 LobbyDirectory Durable Object。

建議目錄：

```text
apps/
  web/       # React 前端
  worker/    # Worker HTTP API、Room Durable Object 及 LobbyDirectory
packages/
  shared/    # 前後端共用型別、房間服務與純遊戲規則
```

## 核心原則

- 伺服器是唯一可信任的遊戲狀態來源。
- 前端只送出玩家意圖，不得自行決定出牌是否合法或修改遊戲結果。
- 伺服器必須驗證回合、手牌所有權、出牌合法性、特殊牌條件及遊戲階段。
- 玩家只能收到自己的完整手牌；其他玩家只公開手牌數量。
- 遊戲規則與房間操作應實作為不依賴 React、Express 或 Socket.IO 的純 TypeScript 邏輯，以便單元測試。
- 前後端共用事件與資料型別，避免重複宣告或使用未驗證的任意物件。
- 優先完成小而可靠的 MVP，不提前加入帳號、資料庫或非必要功能。

## 首版使用流程

### 首頁

- 輸入暱稱。
- 建立新房間。
- 輸入房號加入房間。
- 邀請網址格式可使用 `http://localhost:5173/?room=ABC123`。

### 等候大廳

- 顯示房號及複製邀請連結按鈕。
- 顯示玩家名單、房主、準備及連線狀態。
- 玩家可以準備或取消準備。
- 只有房主可以開始遊戲。
- 至少 2 位玩家且其他玩家均準備後才能開始。
- 房主在大廳離開時，房主身分轉移給下一位玩家。

### 遊戲畫面

- 自己的手牌位於畫面下方。
- 對手顯示暱稱、手牌數量、連線狀態及目前回合標示。
- 中央顯示牌庫、棄牌堆與當前顏色。
- 不合法的牌不可送出或必須顯示明確錯誤。
- 打出萬用牌時必須選擇顏色。
- 提供抽牌、喊 UNO 及抓未喊 UNO 的操作。
- 顯示最近一次遊戲動作。
- 玩家可在牌局中手動開啟或關閉自己的機器人代管。
- 遊戲結束後可以重新開始或離開房間。

## UNO 規則

採經典 108 張牌，不包含新版自訂萬用牌。

必須支援：

- 數字牌。
- 跳過牌。
- 反轉牌。
- 抽二牌。
- 萬用牌。
- 萬用抽四牌。
- 抽牌後若新抽的牌合法，可以立即打出該牌。
- 兩人遊戲中，反轉牌等同跳過牌。
- 萬用抽四牌的合法性判定及質疑流程。
- 玩家剩一張牌時喊 UNO。
- 玩家未喊 UNO 且在下一位玩家完成動作前被抓到時，罰抽兩張。
- 第一張翻開牌的特殊效果。
- 牌庫耗盡時保留目前棄牌，並將其餘棄牌重新洗入牌庫。

首版不支援：

- 抽二與抽四疊加。
- 7-0 規則。
- Jump-in 搶牌。
- 一次打出多張相同牌。
- 自訂規則。
- 觀戰模式。
- 聊天系統。
- 帳號、戰績及排行榜。

## 規則手冊維護

- 網頁必須提供全站隨時可開啟的規則手冊，首頁、大廳與遊戲畫面都不得移除此入口。
- 規則手冊至少必須包含基本規則、各卡片效果、UNO 與抽四質疑流程、起始牌處理及額外玩法。
- 規則手冊實作於 `apps/web/src/RulesGuide.tsx`，顯示內容必須與 `packages/shared/src/game` 的實際規則引擎一致。
- 之後每次新增、移除或修改任何規則、卡片效果、判定方式或額外玩法時，必須在同一項變更中同步更新規則手冊，不可只修改引擎或伺服器。
- 尚未啟用的額外玩法要明確標示為未支援；新增額外玩法後，必須列出玩法名稱、啟用條件、效果及與經典規則不同之處。

## 遊戲狀態

遊戲至少應區分以下階段：

```ts
type GamePhase =
  | "lobby"
  | "playing"
  | "awaiting-draw-four-challenge"
  | "finished";
```

主要狀態包括：

- 房號及房主 ID。
- 玩家順序及連線狀態。
- 每位玩家的私人手牌。
- 牌庫及棄牌堆。
- 當前顏色。
- 當前玩家。
- 遊戲進行方向。
- 遊戲階段。
- UNO 宣告及可被抓取狀態。
- 最後一次操作。
- 勝利玩家。
- 遞增的狀態版本號。

每次狀態更新應包含版本號，前端不得用舊封包覆蓋較新的狀態。

## HTTP 與 WebSocket protocol

HTTP API：

```text
GET  /health
GET  /api/rooms
POST /api/rooms
POST /api/rooms/:roomCode/join
GET  /ws/room/:roomCode
```

WebSocket 第一個訊息必須是 `session:attach`。之後玩家送往伺服器的主要訊息：

```text
room:ready
game:start
game:play-card
game:draw-card
game:call-uno
game:catch-uno
game:challenge-draw-four
game:rematch
game:bot-control
```

伺服器送往玩家的主要訊息：

```text
room:updated
game:started
game:state
game:action-rejected
```

所有訊息使用 `packages/shared/src/room.ts` 的 serializable discriminated union，並由 Worker 執行期驗證。

## 重新連線與離線處理

- 瀏覽器以 localStorage 保存隨機連線權杖；Durable Object 只保存 token hash，不可以只依賴 WebSocket ID。
- 玩家重新整理頁面後應能恢復原本座位與手牌。
- 遊戲中的玩家斷線時立即啟用機器人代管，不暫停其他玩家；座位與手牌保留到玩家重新連線或牌局結束。
- 玩家重新連線後必須自動關閉代管並取回原本座位與手牌；玩家也可在連線正常時手動開啟或關閉自己的代管。
- 代管只接手玩家操作，不得建立新座位、改變手牌所有權、略過規則驗證或取得其他玩家的私人資訊。
- 斷線玩家沒有重連期限，牌局由機器人持續代管，不因斷線逾時取消。
- 牌局中所有真人都離線時應立即清除房間，不讓純機器人房間持續占用記憶體。
- 大廳中的斷線玩家可直接移除。
- 房間無玩家後應自動清理，避免記憶體持續累積。

## 安全與資料驗證

- 房號及玩家權杖使用安全的隨機來源產生。
- 暱稱需要限制長度、去除前後空白並安全顯示。
- 建立及加入房間需要 Worker 邊緣與 Durable Object 的基本頻率限制。
- 所有 HTTP 與 WebSocket payload 都要進行執行期驗證。
- 不接受客戶端傳入完整遊戲狀態、抽牌結果或任意玩家 ID 來覆蓋伺服器資料。
- 錯誤回應應清楚但不可洩漏其他玩家手牌或伺服器內部資料。

## 開發順序

1. 建立 pnpm workspace、前端、Worker 及 shared package。
2. 建立不依賴網路層的 UNO 規則引擎與單元測試。
3. 實作房間建立、加入、準備及開始遊戲。
4. 將規則引擎接入 Room Durable Object，完成 2 人原生 WebSocket 即時遊戲。
5. 完成響應式牌桌、萬用牌選色、UNO 及抽四質疑介面。
6. 擴充及驗證 3–8 人遊戲。
7. 加入重新連線、斷線代管、Durable Object Alarm 及房間清理。
8. 加入 Worker/DO 測試，並使用 OpenChamber web browser tools 驗證主要瀏覽器流程。

## 測試要求

- 測試牌組數量及牌面分布。
- 測試合法與不合法出牌。
- 測試 2–8 人的方向及回合順序。
- 測試所有特殊牌與第一張翻開牌效果。
- 測試抽牌後出牌限制。
- 測試 UNO 宣告、抓取時機及罰牌。
- 測試萬用抽四的合法使用、成功質疑與失敗質疑。
- 測試牌庫重洗及勝利判定。
- 以多個 WebSocket 客戶端測試建房、加入、開始、出牌及斷線重連。
- 使用 OpenChamber web browser tools 驗證至少一條從建房到遊戲結束的完整流程。
- 驗證桌面與手機尺寸都能完成操作。

## 首版驗收標準

1. 2–8 位玩家可從不同瀏覽器加入相同房間。
2. 所有玩家看到一致且即時更新的公開牌桌狀態。
3. 玩家不能查看他人手牌、偽造抽牌或打出不屬於自己的牌。
4. 所有首版 UNO 規則與特殊牌效果正確執行。
5. 玩家重新整理後可恢復原本座位與私人手牌。
6. 玩家出完最後一張牌後正確顯示勝者。
7. 手機與桌面瀏覽器都能完成整場遊戲。
8. 單元測試、Worker/DO 測試及主要端對端流程全部通過。
